import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.datave.fincacamasas",
  appName: "finca-las-camasas",
  webDir: "out",
};

export default config;
